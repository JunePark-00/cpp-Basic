#include<iostream>  // cin cout
#include<string>    // 문자 
#include<time.h>    //시간에 대한 헤더파일
#include<unistd.h> // 콘솔창의 명령어를 사용할 수 있는 헤더파일
#include<stdlib.h>  // malloc , 랜덤함수들 사용할 때 

using namespace std;

// 나만의 헤더파일을 만드는 이유!
// 내가 프로그램을 작성할때 내가 만든 함수,내가만든 클래스,구조체 
// 모아놓은 파일 

void show(){
	cout << "데이터가 잘 들어왔다."<< endl;
}